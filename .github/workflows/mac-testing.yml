name: Manual trigger
on:
  push:
    paths:
      - '.github/workflows/mac-testing.yml'
jobs:
    hello:
        runs-on: [self-hosted, linux, ARM64]
        steps:
        - name: Hello Step
          run: echo "Hello world"